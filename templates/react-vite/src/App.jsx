export default function App() {
  return (
    <main className="page">
      <section className="card">
        <span className="badge">Super Dev Kit</span>
        <h1>{{PROJECT_NAME}}</h1>
        <p>React + Vite pronto para começar.</p>
      </section>
    </main>
  )
}
